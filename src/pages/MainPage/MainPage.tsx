import { Container } from '@/widgets/Container';
import { EventCard } from '@/widgets/EventCard';

export const MainPage = () => {
   return (
      <Container>
         <EventCard />
      </Container>
   );
};
